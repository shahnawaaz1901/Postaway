export default class Token {
  static generateAccessToken(user: string): string {
    return "";
  }
  static generateRefreshToken(user: string): string {
    return "";
  }
  static validateToken(token: string): any {}
}
